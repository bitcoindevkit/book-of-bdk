electrum:
  cd wallet/electrum && cargo run --bin electrum

esplora:
  cd wallet/esplora && cargo run --bin esplora

rpc:
  cd wallet/rpc && cargo run --bin rpc

architecture:
  cd architecture && cargo run --bin architecture

descriptors:
  cd descriptors && cargo run --bin descriptors
