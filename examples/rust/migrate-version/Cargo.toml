[package]
name = "migrate-version"
version = "0.1.0"
edition = "2021"

# --8<-- [start:deps]
[dependencies]
bdk_wallet = { version = "3.0.0", features = ["rusqlite"] }
# --8<-- [end:deps]
anyhow = "1"